name: Claude Review

# The review itself lives in iXsystems/ux-github-workflows, as
# claude-review.yml — the one built in truenas/api-client-ts (schema, rubric,
# threshold gate, thread history, stale banner), moved there so every caller
# shares it and a change to the rubric or the gate happens once.
#
# What moved out of this repo with the switch:
#   the inline claude-code-action review that used to live in this file
#   .github/workflows/check-team.yml -> check-member.yml there, called by it
#
# `.claude/review-prompt.md` stays here, and is what the shared workflow's
# `prompt-file` input points at by default. It says what to look for in *this*
# codebase; the shared rubric is appended after it and says how to grade and
# report what turns up. Do not put severity levels back in it — the gate
# scores the shared ones.
#
# Reports as two checks — these exact strings, which are what branch
# protection would match:
#
#   review / Automatic PR review
#   review / check-member / Check member access
#
# (three segments on the gate: it is a workflow the shared workflow itself
# calls). They replace `check-team / Check Team Membership` and `review`: any
# required-check setting still naming the old ones stays pending forever, and
# the PR is unmergeable with nothing red on it to explain why.

on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

jobs:
  review:
    uses: iXsystems/ux-github-workflows/.github/workflows/claude-review.yml@master
    permissions:
      contents: read
      issues: write
      pull-requests: write
    secrets:
      # Both are mapped on purpose, and the shared workflow prefers the token.
      # `UX_CLAUDE_CODE_OAUTH_TOKEN` is an org secret granted per repository:
      # where the grant is missing it resolves to the empty string rather than
      # failing, so the API key below covers that case and this repo moves to
      # subscription billing on its own once the grant lands. Drop the key line
      # when API billing is retired, not before.
      claude-code-oauth-token: ${{ secrets.UX_CLAUDE_CODE_OAUTH_TOKEN }}
      anthropic-api-key: ${{ secrets.CLAUDE_API_KEY }}
